set(SOURCES GameClientReversiblePlayback.cpp
            GameLoop.cpp)

set(HEADERS GameClientReversiblePlayback.h
            GameLoop.h
            IGameClientPlayback.h)

core_add_library(gameplayback)
add_dependencies(gameplayback libcpluff)
